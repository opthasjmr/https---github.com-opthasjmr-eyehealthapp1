
// This next.config.ts is inside a 'hosting' directory that appears to be a
// duplicate or outdated part of the main BloomVerse project.
// The main project's next.config.ts is at the root level.
// This folder and its contents should likely be removed to avoid confusion.

/** @type {import('next').NextConfig} */
const nextConfig = {
  // This configuration is intentionally left minimal as it's likely not in use.
};

export default nextConfig;
